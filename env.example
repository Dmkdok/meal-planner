# Docker image to deploy (override if pinning a version)
WEB_IMAGE=dmkdok/raskladka:latest

# Database credentials (compose uses these for Postgres and app DSN)
POSTGRES_PASSWORD=change-me

# App secrets and flags
SECRET_KEY=change-me
INIT_DB=false

# Optional: override Flask env (default is production)
# FLASK_ENV=production

# Optional: database URI override (compose sets one targeting the db service)
# DATABASE_URI=postgresql+psycopg://rask_user:${POSTGRES_PASSWORD}@db:5432/raskladka

